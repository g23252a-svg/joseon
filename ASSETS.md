# 에셋 규격서 — 동아

> **에셋 제작자(Codex)를 위한 작업 지시서입니다.**
> 여기 적힌 경로에 여기 적힌 규격대로 PNG·WebP와 지도 데이터를 놓으면 게임이 자동으로 집어 씁니다.
> 파일이 없으면 코드로 그린 임시 그림으로 돌아가므로 **한 장씩 따로 넣어도 됩니다.**
> 게임 코드는 건드릴 필요가 없습니다.

## 공통 규칙

| 항목 | 값 |
|---|---|
| 형식 | UI 시트는 PNG-32(알파), 지도 음영은 WebP |
| 시점 | 지도는 **위에서 내려다본 평면**. 원근 없음 |
| 외곽선 | 아이콘·깃발은 어두운 외곽선 1px 권장 |
| 확대 | 게임이 알아서 줄여 씁니다. **표에 적힌 원본 크기로** 만들어 주십시오 |
| 한자 | **쓰지 마십시오.** 글자가 필요하면 한글로 |

### 색 팔레트

```
지형   평야 #5d6b45   구릉 #6a6247   산지 #585349   연안 #4f6459
세력   조선 #7186c2   중국 #d1b65c   일본 #70967f
바다   #101820        금색 #e0bd76   글자 #e8e1d1
좋음   #8fa86a        나쁨 #c4534a   푸른 #9fd0e8
```

### 분위기

조선 회화의 담백한 채색. 화려한 판타지 금박 금지.
지도는 **실제 음영 지형 + 반투명 정치색**을 겹친 역사 전략 지도 느낌입니다.

---

## 0. `assets/map_geo.js` — 실제 지도 벡터

- 게임의 42개 지방을 실제 동아시아 해안선과 행정구역에 맞춘 `Path2D` 데이터
- 조선 16 · 중국 14 · 일본 9 · 대마도/류큐/대만 3
- 화면과 클릭 판정에 쓰며, 전쟁·인접 계산용 논리 격자는 따로 유지합니다
- 출처: Natural Earth 5.1.2 Admin-1 (public domain)
- 재생성: `python tools/generate_map_geo.py`

## 0-1. `assets/east_asia_relief.webp` — 음영 지형

- **888×480**, `map_geo.js`와 같은 람베르트 정각 원추 투영
- 평야·산맥·사막·하천·연안을 한 장에 담아 정적 지도 바탕으로 씁니다
- 출처: Natural Earth II 3.2.0 (public domain)
- 재생성: `python tools/generate_relief.py --raster <Natural Earth II TIFF>`

---

## 1. `assets/terrain.png` — 지형

- **한 칸 44×44**, 가로 4칸. 최종 **176×44**
- 서로 이어 붙어도 경계가 티나지 않게 (같은 칸이 여러 번 반복됩니다)
- 위에 세력 색이 반투명으로 덮이므로 **너무 진하지 않게**

| 칸 | 지형 |
|:--:|---|
| 0 | 평야 — 논밭, 옅은 녹황 |
| 1 | 구릉 — 낮은 언덕 |
| 2 | 산지 — 뾰족한 능선 |
| 3 | 연안 — 물가, 모래와 잔물결 |

## 2. `assets/icons.png` — 자원 아이콘

- **한 칸 32×32**, 가로 8칸. 최종 **256×32**
- 단색에 가깝게. 어두운 배경 위에 올라갑니다

| 칸 | 아이콘 | 모티프 |
|:--:|---|---|
| 0 | 국고 | 엽전 꾸러미 |
| 1 | 식량 | 볏단 |
| 2 | 인구 | 사람 셋 |
| 3 | 군사 | 창과 방패 |
| 4 | 기술 | 붓과 두루마리 |
| 5 | 고을 | 기와집 |
| 6 | 민심 | 저울 |
| 7 | 불만 | 갈라진 저울 |

## 3. `assets/flags.png` — 세력기

- **한 칸 48×32**, 가로 3칸. 최종 **144×32**
- 깃발 모양(장대 없이 천만)

| 칸 | 세력 | 비고 |
|:--:|---|---|
| 0 | 조선 | 시대에 따라 이름이 바뀌므로 **문자 없이** 문양만 |
| 1 | 중국 | 〃 |
| 2 | 일본 | 〃 |

## 4. `assets/portraits.png` — 신하 초상 *(다음 작업분)*

- **한 칸 64×64**, 가로 8칸 × 세로 2줄. 최종 **512×128**
- 16종을 돌려 씁니다. 관복·갓 차림의 상반신
- 얼굴이 특정 인물로 보이지 않게, 유형만 다르게 (문신·무신·노신·소장 등)

## 5. `assets/ui_frame.png` — 판 장식 *(다음 작업분)*

- **한 칸 192×64**, 가로 3칸. 최종 **576×64**
- 0: 판 머리 장식 / 1: 단추 바탕 / 2: 구분선
- 전통 문양(단청·완자)을 절제해서

---

# v0.2.0에서 새로 필요한 것

v0.2.0에 창(연말 결산 · 삼지선다 사건 · 성적표)이 생겼습니다.
지금은 코드로 그린 네모라, 아래 두 장이 있으면 화면이 크게 달라집니다.
**둘 다 없어도 게임은 그대로 돕니다.**

## 6. `assets/seal.png` — 성적 도장 *(새로 필요)*

시대가 끝날 때마다 **상 · 중 · 하 · 난세** 넷 중 하나로 성적이 매겨지고,
끝나면 여덟 시대의 성적표를 한 장에 놓고 봅니다.
그 옆에 찍히는 **붉은 낙관(落款) 도장**입니다.

- **한 칸 64×64**, 가로 4칸. 최종 **256×64**
- 인주 붉은색 `#a8352c` 계열 한 가지. 종이 위에 찍힌 듯 **가장자리가 조금 번지고 군데군데 비어야** 합니다
- 테두리는 네모 도장(전각) 형태. 글자는 **한글**로

| 칸 | 글자 | 느낌 |
|:--:|---|---|
| 0 | 상 | 획이 굵고 반듯하게 |
| 1 | 중 | 보통 |
| 2 | 하 | 조금 흐리게 |
| 3 | 난세 | 두 글자. 획이 거칠고 도장이 반쯤 뭉개진 |

## 7. `assets/frame_big.png` — 큰 창 테두리 *(새로 필요)*

결산·사건·성적표 창의 테두리입니다. **9분할(9-slice)** 로 늘려 씁니다.

- **한 칸 32×32**, 가로 3칸 × 세로 3줄. 최종 **96×96**
- 배열은 위왼·위·위오른 / 왼·가운데·오른 / 아래왼·아래·아래오른 순서
- 가운데 칸(1,1)은 **거의 투명하게** — 그 위에 글자가 올라갑니다
- 모서리 네 칸에만 문양을 넣고, 변은 단순한 선으로 (늘어나도 티가 안 나야 합니다)
- 어두운 판 위에 올라가므로 **금선 `#e0bd76` 을 옅게**

## 5-1. `assets/ui_frame.png` 에 칸 하나 추가 *(있으면 좋음)*

기존 3칸 뒤에 **4번째 칸**을 붙여 **768×64** 로 만들어 주십시오.

| 칸 | 쓰임 |
|:--:|---|
| 3 | 사건 선택지 한 줄의 바탕 (가로로 길게 늘여 씁니다. 왼쪽 끝에만 작은 표식) |

---

## 작업 순서 제안

1. **`terrain.png`** — 지도 전체 인상이 바뀝니다
2. **`icons.png`** — 상단 표시줄이 읽기 쉬워집니다
3. **`flags.png`**
4. **`seal.png`** — 성적표가 살아납니다 (v0.2.0)
5. **`frame_big.png`** — 창이 살아납니다 (v0.2.0)
6. 나머지는 나중에

## 확인 방법

브라우저 콘솔에 `assetStatus()` 를 치면 무엇이 붙었는지 나옵니다.

```
terrain ✓ 176×44
icons — 없음(코드 그림)
```

## 콘솔의 빨간 줄에 대하여

아직 만들지 않은 시트마다 `Failed to load resource` 가 찍힙니다.
**정상입니다.** 브라우저가 찍는 것이라 코드로 막을 수 없고,
시트를 채울 때마다 하나씩 사라집니다.


---

# 소리 — 넣을 파일이 없습니다

v0.4.0의 효과음과 배경음은 **음원 파일을 쓰지 않습니다.**
Web Audio로 그 자리에서 만들어 냅니다(북 · 징 · 뜯는 줄 · 피리).

- 넣어야 할 `.mp3` `.wav` 가 없습니다
- 배경음은 시대마다 오음계에서 골라 그때그때 지어 내므로, 같은 가락이 두 번 나오지 않습니다
- 크기와 켜고 끄기는 메뉴 → 소리 에서 정하고, 그 설정은 브라우저에 남습니다

소리를 손보려면 `index.html` 의 `SFX`(무슨 소리를 낼 것인가)와
`musEra()`(시대마다의 빛깔)를 고치십시오. 악기 자체는 `sPluck` · `sFlute` ·
`sDrum` · `sGong` 넷입니다.
