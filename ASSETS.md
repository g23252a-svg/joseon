# 에셋 규격서 — 동아

> **에셋 제작자(Codex)를 위한 작업 지시서입니다.**
> 여기 적힌 경로에 여기 적힌 규격대로 PNG를 놓으면 게임이 자동으로 집어 씁니다.
> 파일이 없으면 코드로 그린 임시 그림으로 돌아가므로 **한 장씩 따로 넣어도 됩니다.**
> 게임 코드는 건드릴 필요가 없습니다.

## 공통 규칙

| 항목 | 값 |
|---|---|
| 형식 | UI 시트는 PNG-32, 지도 지형은 WebP |
| 시점 | 지도는 **위에서 내려다본 평면**. 원근 없음 |
| 외곽선 | 아이콘·깃발은 어두운 외곽선 1px 권장 |
| 확대 | 게임이 알아서 줄여 씁니다. **표에 적힌 원본 크기로** 만들어 주십시오 |
| 한자 | **쓰지 마십시오.** 글자가 필요하면 한글로 |

### 색 팔레트

```
지형   평야 #5d6b45   구릉 #6a6247   산지 #585349   연안 #4f6459
세력   조선 #4f7fb5   중국 #b5654f   일본 #8a6fae
바다   #101820        금색 #e0bd76   글자 #e8e1d1
좋음   #8fa86a        나쁨 #c4534a   푸른 #9fd0e8
```

### 분위기

지도는 실제 지표색과 고도 음영을 먼저 보여 주고 정치색을 반투명으로 얹습니다.
전체 화면에서는 정치 지도를, 근접 화면에서는 산맥·고원·평야가 읽히는 지형 지도를 지향합니다.

---

## 0. `assets/map_geo.js` — 실제 지리 벡터

- 게임 화면과 클릭 판정에 쓰는 26개 권역의 `Path2D` 데이터
- Natural Earth 5.1.2 공개 도메인 1:10m 행정구역을 병합·투영·단순화한 결과
- 경제·전쟁용 논리 격자와 분리되어 있어 지도를 바꿔도 게임 수치는 유지됩니다
- 재생성: `tools/generate_map_geo.py --admin1 <원본.geojson> --out assets/map_geo.js`

현재 지도는 이 벡터를 직접 채색하므로 `terrain.png`를 셀마다 반복하지 않습니다.
아래 PNG 규격은 다른 UI와 향후 지형 오버레이 제작을 위해 유지합니다.

## 0-1. `assets/east_asia_relief.webp` — 실제 지형 베이스

- **968×572**, 현재 지도 뷰포트와 1:1 대응
- Natural Earth II 3.2.0의 공개 도메인 지표색·음영기복 래스터 사용
- `map_geo.js`와 같은 Lambert 정각 원추 도법으로 재투영해 해안선이 일치합니다
- 게임에서는 지형 모드 84%, 정치 모드 48% 정도 보이도록 지형과 세력색을 겹칩니다
- 재생성: `tools/generate_relief.py --admin1 <원본.geojson> --raster <원본.tif> --out assets/east_asia_relief.webp`

---

## 1. `assets/terrain.png` — 지형

- **한 칸 44×44**, 가로 4칸. 최종 **176×44**
- 서로 이어 붙어도 경계가 티나지 않게 (같은 칸이 여러 번 반복됩니다)
- 위에 세력 색이 반투명으로 덮이므로 **너무 진하지 않게**

| 칸 | 지형 |
|:--:|---|
| 0 | 평야 — 논밭, 옅은 녹황 |
| 1 | 구릉 — 낮은 언덕 |
| 2 | 산지 — 뾰족한 능선 |
| 3 | 연안 — 물가, 모래와 잔물결 |

## 2. `assets/icons.png` — 자원 아이콘

- **한 칸 32×32**, 가로 8칸. 최종 **256×32**
- 단색에 가깝게. 어두운 배경 위에 올라갑니다

| 칸 | 아이콘 | 모티프 |
|:--:|---|---|
| 0 | 국고 | 엽전 꾸러미 |
| 1 | 식량 | 볏단 |
| 2 | 인구 | 사람 셋 |
| 3 | 군사 | 창과 방패 |
| 4 | 기술 | 붓과 두루마리 |
| 5 | 고을 | 기와집 |
| 6 | 민심 | 저울 |
| 7 | 불만 | 갈라진 저울 |

## 3. `assets/flags.png` — 세력기

- **한 칸 48×32**, 가로 3칸. 최종 **144×32**
- 깃발 모양(장대 없이 천만)

| 칸 | 세력 | 비고 |
|:--:|---|---|
| 0 | 조선 | 시대에 따라 이름이 바뀌므로 **문자 없이** 문양만 |
| 1 | 중국 | 〃 |
| 2 | 일본 | 〃 |

## 4. `assets/portraits.png` — 신하 초상 *(다음 작업분)*

- **한 칸 64×64**, 가로 8칸 × 세로 2줄. 최종 **512×128**
- 16종을 돌려 씁니다. 관복·갓 차림의 상반신
- 얼굴이 특정 인물로 보이지 않게, 유형만 다르게 (문신·무신·노신·소장 등)

## 5. `assets/ui_frame.png` — 판 장식 *(다음 작업분)*

- **한 칸 192×64**, 가로 3칸. 최종 **576×64**
- 0: 판 머리 장식 / 1: 단추 바탕 / 2: 구분선
- 전통 문양(단청·완자)을 절제해서

---

## 작업 순서 제안

1. **`terrain.png`** — 지도 전체 인상이 바뀝니다
2. **`icons.png`** — 상단 표시줄이 읽기 쉬워집니다
3. **`flags.png`**
4. 4·5번은 나중에

## 확인 방법

브라우저 콘솔에 `assetStatus()` 를 치면 무엇이 붙었는지 나옵니다.

```
terrain ✓ 176×44
icons — 없음(코드 그림)
```

## 콘솔의 빨간 줄에 대하여

아직 만들지 않은 시트마다 `Failed to load resource` 가 찍힙니다.
**정상입니다.** 브라우저가 찍는 것이라 코드로 막을 수 없고,
시트를 채울 때마다 하나씩 사라집니다.
